declare module 'lucide-react-native/dist/esm/icons/*.js' {
  import type React from 'react';
  const Icon: React.ComponentType<{ color?: string; size?: number; strokeWidth?: number }>;
  export default Icon;
}
